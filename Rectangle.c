#include <stdio.h>
int height;
int width;

int main () { 

    printf("enter height: \n");
    scanf(" %d", height);
    printf("enter width: \n");
    scanf("%d", width);

    area = height*width;
    printf("area of rectangle = %d cm\n", area);

    if (height = width){
        if (height = width)
        printf("%d is a square", square);
        else 
        printf("%d is a rectangle", rectangle);
    }
return 0;
    
}
